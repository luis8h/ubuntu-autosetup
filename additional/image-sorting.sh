#!/bin/bash

# image duplicates
sudo apt install findimagedupes -y

# heic support
# additionaly install eye of gnome in ubuntu software store
sudo apt install heif-thumbnailer -y
sudo apt install heif-gdk-pixbuf -y
